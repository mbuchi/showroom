// Draft validation against the IDX 3.01 must-field matrix. STUB: the engine
// task replaces the body. See docs/plans/2026-07-29-portal-publisher.md.
import type { IdxIssue, ListingDraft } from './types';

export function validateDraft(draft: ListingDraft): IdxIssue[] {
  void draft;
  return [];
}
